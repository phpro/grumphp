parameters:

  bin_dir: "./vendor/bin"
  git_dir: "."
  tasks:
      phpcs:
        standard: PSR2
        ignore_patterns:
          - "spec/*Spec.php"
          - "test/*.php"
      phpspec: ~
      phpunit: ~
      composer:
        no_check_lock: true
        strict: true
